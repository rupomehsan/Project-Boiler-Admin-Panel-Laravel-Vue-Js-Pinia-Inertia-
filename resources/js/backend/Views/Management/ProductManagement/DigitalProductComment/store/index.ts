import { createCrudStore } from "@/shared/store/createStore";
import setup from "../setup";

export const digital_product_comment_store = createCrudStore(setup);
export const store = digital_product_comment_store;
export default digital_product_comment_store;
